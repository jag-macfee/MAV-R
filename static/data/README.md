# Using reference (digitised) data

This file is just to introduce customs in using digitised data.

`Plotter` assumes the data is provided in a .csv file. The x-values column should be labeled `x` and likewise `y` for the y-values. You provide `Plotter` with the necessary x/y-labels.

Thanks :)
